{ ... }: {
  programs.ghostty = {
    enable = true;

    settings = {
      # theme = "Batman";
      # theme = "Hardcore";
      # theme = "NvimDark";
      # theme = "Brogrammer"; # -> So good
      theme = "Guezwhoz"; # -> get alone with the system better
      font-size = 12;
    };
  };
}